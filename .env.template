# === CVAT Server Connection ===
CVAT_HOST=http://localhost:8080
CVAT_USERNAME=your_cvat_username
CVAT_PASSWORD=your_cvat_password

# === CVAT Task Configuration ===
CVAT_TASK_NAME=CVAT Task - Pothole
CVAT_LABEL_NAME=pothole
CVAT_TASK_ID=1
CVAT_EXPORT_FORMAT=COCO 1.0

# === Paths for Image Upload and Annotation Schema ===
IMAGE_DIR=path/to/images/train
ANNOTATION_FILE=path/to/annotations.json

# === Exported Annotation Output (COCO JSON) ===
EXPORT_DIR=path/to/output/labels
EXPORT_FILENAME=batch_1.json

# === COCO to YOLO Conversion ===
COCO_JSON_PATH=path/to/exported_coco.json
YOLO_LABELS_OUTPUT_DIR=path/to/yolo/labels/train

# === YOLO Training Config ===
DATA_YAML_PATH=path/to/data.yaml
N_CLASSES=1
CLASS_NAMES=['pothole']
